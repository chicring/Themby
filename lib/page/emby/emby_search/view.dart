import 'package:flutter/material.dart';

class EmbySearch extends StatelessWidget {
  const EmbySearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Emby Search'),
      ),
    );
  }
}